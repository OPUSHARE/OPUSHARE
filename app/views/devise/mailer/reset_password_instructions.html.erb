<p>こんにちは <%= @resource.email %>さん</p>

<p>パスワードの変更は以下のリンクを通して行ってください。</p>

<p><%= link_to 'パスワードを変更', edit_password_url(@resource, :reset_password_token => @token) %></p>

<p>このメールに憶えの無い方は無視してください。</p>
